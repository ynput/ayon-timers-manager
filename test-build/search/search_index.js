var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"AYON Timers Manager Addon API Reference","text":"<p>Unified API for timers management to easily start/stop timer across multiple addons.</p>"},{"location":"license.html","title":"License","text":"<pre><code>                             Apache License\n                       Version 2.0, January 2004\n                    http://www.apache.org/licenses/\n</code></pre> <p>TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION</p> <ol> <li> <p>Definitions.</p> <p>\"License\" shall mean the terms and conditions for use, reproduction,   and distribution as defined by Sections 1 through 9 of this document.</p> <p>\"Licensor\" shall mean the copyright owner or entity authorized by   the copyright owner that is granting the License.</p> <p>\"Legal Entity\" shall mean the union of the acting entity and all   other entities that control, are controlled by, or are under common   control with that entity. For the purposes of this definition,   \"control\" means (i) the power, direct or indirect, to cause the   direction or management of such entity, whether by contract or   otherwise, or (ii) ownership of fifty percent (50%) or more of the   outstanding shares, or (iii) beneficial ownership of such entity.</p> <p>\"You\" (or \"Your\") shall mean an individual or Legal Entity   exercising permissions granted by this License.</p> <p>\"Source\" form shall mean the preferred form for making modifications,   including but not limited to software source code, documentation   source, and configuration files.</p> <p>\"Object\" form shall mean any form resulting from mechanical   transformation or translation of a Source form, including but   not limited to compiled object code, generated documentation,   and conversions to other media types.</p> <p>\"Work\" shall mean the work of authorship, whether in Source or   Object form, made available under the License, as indicated by a   copyright notice that is included in or attached to the work   (an example is provided in the Appendix below).</p> <p>\"Derivative Works\" shall mean any work, whether in Source or Object   form, that is based on (or derived from) the Work and for which the   editorial revisions, annotations, elaborations, or other modifications   represent, as a whole, an original work of authorship. For the purposes   of this License, Derivative Works shall not include works that remain   separable from, or merely link (or bind by name) to the interfaces of,   the Work and Derivative Works thereof.</p> <p>\"Contribution\" shall mean any work of authorship, including   the original version of the Work and any modifications or additions   to that Work or Derivative Works thereof, that is intentionally   submitted to Licensor for inclusion in the Work by the copyright owner   or by an individual or Legal Entity authorized to submit on behalf of   the copyright owner. For the purposes of this definition, \"submitted\"   means any form of electronic, verbal, or written communication sent   to the Licensor or its representatives, including but not limited to   communication on electronic mailing lists, source code control systems,   and issue tracking systems that are managed by, or on behalf of, the   Licensor for the purpose of discussing and improving the Work, but   excluding communication that is conspicuously marked or otherwise   designated in writing by the copyright owner as \"Not a Contribution.\"</p> <p>\"Contributor\" shall mean Licensor and any individual or Legal Entity   on behalf of whom a Contribution has been received by Licensor and   subsequently incorporated within the Work.</p> </li> <li> <p>Grant of Copyright License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       copyright license to reproduce, prepare Derivative Works of,       publicly display, publicly perform, sublicense, and distribute the       Work and such Derivative Works in Source or Object form.</p> </li> <li> <p>Grant of Patent License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       (except as stated in this section) patent license to make, have made,       use, offer to sell, sell, import, and otherwise transfer the Work,       where such license applies only to those patent claims licensable       by such Contributor that are necessarily infringed by their       Contribution(s) alone or by combination of their Contribution(s)       with the Work to which such Contribution(s) was submitted. If You       institute patent litigation against any entity (including a       cross-claim or counterclaim in a lawsuit) alleging that the Work       or a Contribution incorporated within the Work constitutes direct       or contributory patent infringement, then any patent licenses       granted to You under this License for that Work shall terminate       as of the date such litigation is filed.</p> </li> <li> <p>Redistribution. You may reproduce and distribute copies of the       Work or Derivative Works thereof in any medium, with or without       modifications, and in Source or Object form, provided that You       meet the following conditions:</p> <p>(a) You must give any other recipients of the Work or       Derivative Works a copy of this License; and</p> <p>(b) You must cause any modified files to carry prominent notices       stating that You changed the files; and</p> <p>(c) You must retain, in the Source form of any Derivative Works       that You distribute, all copyright, patent, trademark, and       attribution notices from the Source form of the Work,       excluding those notices that do not pertain to any part of       the Derivative Works; and</p> <p>(d) If the Work includes a \"NOTICE\" text file as part of its       distribution, then any Derivative Works that You distribute must       include a readable copy of the attribution notices contained       within such NOTICE file, excluding those notices that do not       pertain to any part of the Derivative Works, in at least one       of the following places: within a NOTICE text file distributed       as part of the Derivative Works; within the Source form or       documentation, if provided along with the Derivative Works; or,       within a display generated by the Derivative Works, if and       wherever such third-party notices normally appear. The contents       of the NOTICE file are for informational purposes only and       do not modify the License. You may add Your own attribution       notices within Derivative Works that You distribute, alongside       or as an addendum to the NOTICE text from the Work, provided       that such additional attribution notices cannot be construed       as modifying the License.</p> <p>You may add Your own copyright statement to Your modifications and   may provide additional or different license terms and conditions   for use, reproduction, or distribution of Your modifications, or   for any such Derivative Works as a whole, provided Your use,   reproduction, and distribution of the Work otherwise complies with   the conditions stated in this License.</p> </li> <li> <p>Submission of Contributions. Unless You explicitly state otherwise,       any Contribution intentionally submitted for inclusion in the Work       by You to the Licensor shall be under the terms and conditions of       this License, without any additional terms or conditions.       Notwithstanding the above, nothing herein shall supersede or modify       the terms of any separate license agreement you may have executed       with Licensor regarding such Contributions.</p> </li> <li> <p>Trademarks. This License does not grant permission to use the trade       names, trademarks, service marks, or product names of the Licensor,       except as required for reasonable and customary use in describing the       origin of the Work and reproducing the content of the NOTICE file.</p> </li> <li> <p>Disclaimer of Warranty. Unless required by applicable law or       agreed to in writing, Licensor provides the Work (and each       Contributor provides its Contributions) on an \"AS IS\" BASIS,       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or       implied, including, without limitation, any warranties or conditions       of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A       PARTICULAR PURPOSE. You are solely responsible for determining the       appropriateness of using or redistributing the Work and assume any       risks associated with Your exercise of permissions under this License.</p> </li> <li> <p>Limitation of Liability. In no event and under no legal theory,       whether in tort (including negligence), contract, or otherwise,       unless required by applicable law (such as deliberate and grossly       negligent acts) or agreed to in writing, shall any Contributor be       liable to You for damages, including any direct, indirect, special,       incidental, or consequential damages of any character arising as a       result of this License or out of the use or inability to use the       Work (including but not limited to damages for loss of goodwill,       work stoppage, computer failure or malfunction, or any and all       other commercial damages or losses), even if such Contributor       has been advised of the possibility of such damages.</p> </li> <li> <p>Accepting Warranty or Additional Liability. While redistributing       the Work or Derivative Works thereof, You may choose to offer,       and charge a fee for, acceptance of support, warranty, indemnity,       or other liability obligations and/or rights consistent with this       License. However, in accepting such obligations, You may act only       on Your own behalf and on Your sole responsibility, not on behalf       of any other Contributor, and only if You agree to indemnify,       defend, and hold each Contributor harmless for any liability       incurred by, or claims asserted against, such Contributor by reason       of your accepting any such warranty or additional liability.</p> </li> </ol> <p>END OF TERMS AND CONDITIONS</p> <p>APPENDIX: How to apply the Apache License to your work.</p> <pre><code>  To apply the Apache License to your work, attach the following\n  boilerplate notice, with the fields enclosed by brackets \"[]\"\n  replaced with your own identifying information. (Don't include\n  the brackets!)  The text should be enclosed in the appropriate\n  comment syntax for the file format. We also recommend that a\n  file or class name and description of purpose be included on the\n  same \"printed page\" as the copyright notice for easier\n  identification within third-party archives.\n</code></pre> <p>Copyright [yyyy][name of copyright owner]</p> <p>Licensed under the Apache License, Version 2.0 (the \"License\");    you may not use this file except in compliance with the License.    You may obtain a copy of the License at</p> <pre><code>   http://www.apache.org/licenses/LICENSE-2.0\n</code></pre> <p>Unless required by applicable law or agreed to in writing, software    distributed under the License is distributed on an \"AS IS\" BASIS,    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.    See the License for the specific language governing permissions and    limitations under the License.</p>"},{"location":"autoapi/summary.html","title":"Summary","text":"<ul> <li>client<ul> <li>ayon_timers_manager<ul> <li>exceptions</li> <li>idle_threads</li> <li>launch_hooks<ul> <li>post_start_timer</li> </ul> </li> <li>plugins<ul> <li>publish<ul> <li>start_timer</li> <li>stop_timer</li> </ul> </li> </ul> </li> <li>rest_api</li> <li>timers_manager</li> <li>version</li> <li>widget_user_idle</li> </ul> </li> </ul> </li> <li>server<ul> <li>settings</li> </ul> </li> </ul>"},{"location":"autoapi/client/ayon_timers_manager/index.html","title":"ayon_timers_manager","text":""},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager","title":"<code>TimersManager</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>ITrayService</code>, <code>IPluginPaths</code></p> <p>Handles about Timers.</p> <p>Should be able to start/stop all timers at once.</p> <p>To be able use this advantage module has to have attribute with name <code>timers_manager_connector</code> which has two methods 'stop_timer' and 'start_timer'. Optionally may have <code>register_timers_manager</code> where object of TimersManager module is passed to be able call it's callbacks.</p> <p>See <code>ExampleTimersManagerConnector</code>.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>class TimersManager(\n    AYONAddon,\n    ITrayService,\n    IPluginPaths\n):\n    \"\"\" Handles about Timers.\n\n    Should be able to start/stop all timers at once.\n\n    To be able use this advantage module has to have attribute with name\n    `timers_manager_connector` which has two methods 'stop_timer'\n    and 'start_timer'. Optionally may have `register_timers_manager` where\n    object of TimersManager module is passed to be able call it's callbacks.\n\n    See `ExampleTimersManagerConnector`.\n    \"\"\"\n    name = \"timers_manager\"\n    version = __version__\n    label = \"Timers Service\"\n\n    _required_methods = (\n        \"stop_timer\",\n        \"start_timer\"\n    )\n\n    def initialize(self, studio_settings):\n        timers_settings = studio_settings.get(self.name)\n        enabled = timers_settings is not None\n\n        auto_stop = False\n        full_time = 0\n        message_time = 0\n        if enabled:\n            # When timer will stop if idle manager is running (minutes)\n            full_time = int(timers_settings[\"full_time\"] * 60)\n            # How many minutes before the timer is stopped will popup the message\n            message_time = int(timers_settings[\"message_time\"] * 60)\n\n            auto_stop = timers_settings[\"auto_stop\"]\n            platform_name = platform.system().lower()\n            # Turn of auto stop on MacOs because pynput requires root permissions\n            #    and on linux can cause thread locks on application close\n            if full_time &lt;= 0 or platform_name in (\"darwin\", \"linux\"):\n                auto_stop = False\n\n        self.enabled = enabled\n        self.auto_stop = auto_stop\n        self.time_show_message = full_time - message_time\n        self.time_stop_timer = full_time\n\n        self.is_running = False\n        self.last_task = None\n\n        # Tray attributes\n        self._signal_handler = None\n        self._widget_user_idle = None\n        self._idle_manager = None\n\n        self._connectors_by_module_id = {}\n        self._modules_by_id = {}\n\n    def tray_init(self):\n        if not self.auto_stop:\n            return\n\n        from .idle_threads import IdleManager\n        from .widget_user_idle import WidgetUserIdle, SignalHandler\n\n        signal_handler = SignalHandler(self)\n        idle_manager = IdleManager()\n        widget_user_idle = WidgetUserIdle(self)\n        widget_user_idle.set_countdown_start(\n            self.time_stop_timer - self.time_show_message\n        )\n\n        idle_manager.signal_reset_timer.connect(\n            widget_user_idle.reset_countdown\n        )\n        idle_manager.add_time_signal(\n            self.time_show_message, signal_handler.signal_show_message\n        )\n        idle_manager.add_time_signal(\n            self.time_stop_timer, signal_handler.signal_stop_timers\n        )\n\n        self._signal_handler = signal_handler\n        self._widget_user_idle = widget_user_idle\n        self._idle_manager = idle_manager\n\n    def tray_start(self, *_a, **_kw):\n        if self._idle_manager:\n            self._idle_manager.start()\n\n    def tray_exit(self):\n        if self._idle_manager:\n            self._idle_manager.stop()\n            self._idle_manager.wait()\n\n    def get_timer_data_for_path(self, task_path):\n        \"\"\"Convert string path to a timer data.\n\n        It is expected that first item is project name, last item is task name\n        and folder path in the middle.\n        \"\"\"\n        path_items = task_path.split(\"/\")\n        task_name = path_items.pop(-1)\n        project_name = path_items.pop(0)\n        folder_path = \"/\" + \"/\".join(path_items)\n        return self.get_timer_data_for_context(\n            project_name, folder_path, task_name, self.log\n        )\n\n    def get_launch_hook_paths(self):\n        \"\"\"Implementation for applications launch hooks.\"\"\"\n\n        return [\n            os.path.join(TIMER_MODULE_DIR, \"launch_hooks\")\n        ]\n\n    def get_plugin_paths(self):\n        \"\"\"Implementation of `IPluginPaths`.\"\"\"\n\n        return {\n            \"publish\": [os.path.join(TIMER_MODULE_DIR, \"plugins\", \"publish\")]\n        }\n\n    @staticmethod\n    def get_timer_data_for_context(\n        project_name, folder_path, task_name, logger=None\n    ):\n        \"\"\"Prepare data for timer related callbacks.\"\"\"\n        if not project_name or not folder_path or not task_name:\n            raise InvalidContextError((\n                \"Missing context information got\"\n                \" Project: \\\"{}\\\" Folder: \\\"{}\\\" Task: \\\"{}\\\"\"\n            ).format(str(project_name), str(folder_path), str(task_name)))\n\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name,\n            folder_path,\n            fields={\"id\", \"name\", \"path\"}\n        )\n\n        if not folder_entity:\n            raise InvalidContextError((\n                \"Folder \\\"{}\\\" not found in project \\\"{}\\\"\"\n            ).format(folder_path, project_name))\n\n        folder_id = folder_entity[\"id\"]\n        task_entity = ayon_api.get_task_by_name(\n            project_name, folder_id, task_name\n        )\n        if not task_entity:\n            raise InvalidContextError((\n                \"Task \\\"{}\\\" not found on folder \\\"{}\\\" in project \\\"{}\\\"\"\n            ).format(task_name, folder_path, project_name))\n\n        task_type = \"\"\n        try:\n            task_type = task_entity[\"taskType\"]\n        except KeyError:\n            msg = \"Couldn't find task_type for {}\".format(task_name)\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n\n        hierarchy_items = folder_entity[\"path\"].split(\"/\")\n        hierarchy_items.pop(0)\n\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"folder_path\": folder_entity[\"path\"],\n            \"task_name\": task_name,\n            \"task_type\": task_type,\n            \"asset_id\": folder_id,\n            \"asset_name\": folder_entity[\"name\"],\n            \"hierarchy\": hierarchy_items,\n        }\n\n    def start_timer(self, project_name, folder_path, task_name):\n        \"\"\"Start timer for passed context.\n\n        Args:\n            project_name (str): Project name.\n            folder_path (str): Folder path.\n            task_name (str): Task name.\n        \"\"\"\n        data = self.get_timer_data_for_context(\n            project_name, folder_path, task_name, self.log\n        )\n        self.timer_started(None, data)\n\n    def get_task_time(self, project_name, folder_path, task_name):\n        \"\"\"Get total time for passed context.\n\n        TODO:\n        - convert context to timer data\n        \"\"\"\n        times = {}\n        for module_id, connector in self._connectors_by_module_id.items():\n            if hasattr(connector, \"get_task_time\"):\n                module = self._modules_by_id[module_id]\n                times[module.name] = connector.get_task_time(\n                    project_name, folder_path, task_name\n                )\n        return times\n\n    def timer_started(self, source_id, data):\n        \"\"\"Connector triggered that timer has started.\n\n        New timer has started for context in data.\n        \"\"\"\n        for module_id, connector in self._connectors_by_module_id.items():\n            if module_id == source_id:\n                continue\n\n            try:\n                connector.start_timer(data)\n            except Exception:\n                self.log.info(\n                    \"Failed to start timer on connector {}\".format(\n                        str(connector)\n                    )\n                )\n\n        self.last_task = data\n        self.is_running = True\n\n    def timer_stopped(self, source_id):\n        \"\"\"Connector triggered that hist timer has stopped.\n\n        Should stop all other timers.\n\n        TODO:\n        - pass context for which timer has stopped to validate if timers are\n            same and valid\n        \"\"\"\n        for module_id, connector in self._connectors_by_module_id.items():\n            if module_id == source_id:\n                continue\n\n            try:\n                connector.stop_timer()\n            except Exception:\n                self.log.info(\n                    \"Failed to stop timer on connector {}\".format(\n                        str(connector)\n                    )\n                )\n\n    def restart_timers(self):\n        if self.last_task is not None:\n            self.timer_started(None, self.last_task)\n\n    def stop_timers(self):\n        \"\"\"Stop all timers.\"\"\"\n        if self.is_running is False:\n            return\n\n        if self._widget_user_idle is not None:\n            self._widget_user_idle.set_timer_stopped()\n        self.is_running = False\n\n        self.timer_stopped(None)\n\n    def connect_with_addons(self, enabled_modules):\n        for module in enabled_modules:\n            connector = getattr(module, \"timers_manager_connector\", None)\n            if connector is None:\n                continue\n\n            missing_methods = set()\n            for method_name in self._required_methods:\n                if not hasattr(connector, method_name):\n                    missing_methods.add(method_name)\n\n            if missing_methods:\n                joined = \", \".join(\n                    ['\"{}\"'.format(name for name in missing_methods)]\n                )\n                self.log.info((\n                    \"Module \\\"{}\\\" has missing required methods {}.\"\n                ).format(module.name, joined))\n                continue\n\n            self._connectors_by_module_id[module.id] = connector\n            self._modules_by_id[module.id] = module\n\n            # Optional method\n            if hasattr(connector, \"register_timers_manager\"):\n                try:\n                    connector.register_timers_manager(self)\n                except Exception:\n                    self.log.info((\n                        \"Failed to register timers manager\"\n                        \" for connector of module \\\"{}\\\".\"\n                    ).format(module.name))\n\n    def show_message(self):\n        if self.is_running is False:\n            return\n        if not self._widget_user_idle.is_showed():\n            self._widget_user_idle.reset_countdown()\n            self._widget_user_idle.show()\n\n    # Webserver module implementation\n    def webserver_initialization(self, server_manager):\n        \"\"\"Add routes for timers to be able start/stop with rest api.\"\"\"\n        if self.tray_initialized:\n            from .rest_api import TimersManagerModuleRestApi\n            self.rest_api_obj = TimersManagerModuleRestApi(\n                self, server_manager\n            )\n\n    @staticmethod\n    def start_timer_with_webserver(\n        project_name, folder_path, task_name, logger=None\n    ):\n        \"\"\"Prepared method for calling change timers on REST api.\n\n        Webserver must be active. At the moment is Webserver running only when\n        OpenPype Tray is used.\n\n        Args:\n            project_name (str): Project name.\n            folder_path (str): Folder path.\n            task_name (str): Task name.\n            logger (logging.Logger): Logger object. Using 'print' if not\n                passed.\n        \"\"\"\n\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n        if not webserver_url:\n            msg = \"Couldn't find webserver url\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        rest_api_url = \"{}/timers_manager/start_timer\".format(webserver_url)\n        try:\n            import requests\n        except Exception:\n            msg = \"Couldn't start timer ('requests' is not available)\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n        data = {\n            \"project_name\": project_name,\n            \"folder_path\": folder_path,\n            \"task_name\": task_name\n        }\n\n        return requests.post(rest_api_url, json=data)\n\n    @staticmethod\n    def stop_timer_with_webserver(logger=None):\n        \"\"\"Prepared method for calling stop timers on REST api.\n\n        Args:\n            logger (logging.Logger): Logger used for logging messages.\n        \"\"\"\n\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n        if not webserver_url:\n            msg = \"Couldn't find webserver url\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        rest_api_url = \"{}/timers_manager/stop_timer\".format(webserver_url)\n        try:\n            import requests\n        except Exception:\n            msg = \"Couldn't start timer ('requests' is not available)\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        return requests.post(rest_api_url)\n\n    def on_host_install(self, host, host_name, project_name):\n        self.log.debug(\"Installing task changed callback\")\n        register_event_callback(\"taskChanged\", self._on_host_task_change)\n\n    def _on_host_task_change(self, event):\n        project_name = event[\"project_name\"]\n        folder_path = event[\"folder_path\"]\n        task_name = event[\"task_name\"]\n        self.log.debug((\n            \"Sending message that timer should change to\"\n            \" Project: {} Folder: {} Task: {}\"\n        ).format(project_name, folder_path, task_name))\n\n        self.start_timer_with_webserver(\n            project_name, folder_path, task_name, self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.get_launch_hook_paths","title":"<code>get_launch_hook_paths()</code>","text":"<p>Implementation for applications launch hooks.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_launch_hook_paths(self):\n    \"\"\"Implementation for applications launch hooks.\"\"\"\n\n    return [\n        os.path.join(TIMER_MODULE_DIR, \"launch_hooks\")\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.get_plugin_paths","title":"<code>get_plugin_paths()</code>","text":"<p>Implementation of <code>IPluginPaths</code>.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_plugin_paths(self):\n    \"\"\"Implementation of `IPluginPaths`.\"\"\"\n\n    return {\n        \"publish\": [os.path.join(TIMER_MODULE_DIR, \"plugins\", \"publish\")]\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.get_task_time","title":"<code>get_task_time(project_name, folder_path, task_name)</code>","text":"<p>Get total time for passed context.</p> <p>TODO: - convert context to timer data</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_task_time(self, project_name, folder_path, task_name):\n    \"\"\"Get total time for passed context.\n\n    TODO:\n    - convert context to timer data\n    \"\"\"\n    times = {}\n    for module_id, connector in self._connectors_by_module_id.items():\n        if hasattr(connector, \"get_task_time\"):\n            module = self._modules_by_id[module_id]\n            times[module.name] = connector.get_task_time(\n                project_name, folder_path, task_name\n            )\n    return times\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.get_timer_data_for_context","title":"<code>get_timer_data_for_context(project_name, folder_path, task_name, logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepare data for timer related callbacks.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef get_timer_data_for_context(\n    project_name, folder_path, task_name, logger=None\n):\n    \"\"\"Prepare data for timer related callbacks.\"\"\"\n    if not project_name or not folder_path or not task_name:\n        raise InvalidContextError((\n            \"Missing context information got\"\n            \" Project: \\\"{}\\\" Folder: \\\"{}\\\" Task: \\\"{}\\\"\"\n        ).format(str(project_name), str(folder_path), str(task_name)))\n\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name,\n        folder_path,\n        fields={\"id\", \"name\", \"path\"}\n    )\n\n    if not folder_entity:\n        raise InvalidContextError((\n            \"Folder \\\"{}\\\" not found in project \\\"{}\\\"\"\n        ).format(folder_path, project_name))\n\n    folder_id = folder_entity[\"id\"]\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_id, task_name\n    )\n    if not task_entity:\n        raise InvalidContextError((\n            \"Task \\\"{}\\\" not found on folder \\\"{}\\\" in project \\\"{}\\\"\"\n        ).format(task_name, folder_path, project_name))\n\n    task_type = \"\"\n    try:\n        task_type = task_entity[\"taskType\"]\n    except KeyError:\n        msg = \"Couldn't find task_type for {}\".format(task_name)\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n\n    hierarchy_items = folder_entity[\"path\"].split(\"/\")\n    hierarchy_items.pop(0)\n\n    return {\n        \"project_name\": project_name,\n        \"folder_id\": folder_id,\n        \"folder_path\": folder_entity[\"path\"],\n        \"task_name\": task_name,\n        \"task_type\": task_type,\n        \"asset_id\": folder_id,\n        \"asset_name\": folder_entity[\"name\"],\n        \"hierarchy\": hierarchy_items,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.get_timer_data_for_path","title":"<code>get_timer_data_for_path(task_path)</code>","text":"<p>Convert string path to a timer data.</p> <p>It is expected that first item is project name, last item is task name and folder path in the middle.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_timer_data_for_path(self, task_path):\n    \"\"\"Convert string path to a timer data.\n\n    It is expected that first item is project name, last item is task name\n    and folder path in the middle.\n    \"\"\"\n    path_items = task_path.split(\"/\")\n    task_name = path_items.pop(-1)\n    project_name = path_items.pop(0)\n    folder_path = \"/\" + \"/\".join(path_items)\n    return self.get_timer_data_for_context(\n        project_name, folder_path, task_name, self.log\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.start_timer","title":"<code>start_timer(project_name, folder_path, task_name)</code>","text":"<p>Start timer for passed context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def start_timer(self, project_name, folder_path, task_name):\n    \"\"\"Start timer for passed context.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n    \"\"\"\n    data = self.get_timer_data_for_context(\n        project_name, folder_path, task_name, self.log\n    )\n    self.timer_started(None, data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.start_timer_with_webserver","title":"<code>start_timer_with_webserver(project_name, folder_path, task_name, logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepared method for calling change timers on REST api.</p> <p>Webserver must be active. At the moment is Webserver running only when OpenPype Tray is used.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>logger</code> <code>Logger</code> <p>Logger object. Using 'print' if not passed.</p> <code>None</code> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef start_timer_with_webserver(\n    project_name, folder_path, task_name, logger=None\n):\n    \"\"\"Prepared method for calling change timers on REST api.\n\n    Webserver must be active. At the moment is Webserver running only when\n    OpenPype Tray is used.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        logger (logging.Logger): Logger object. Using 'print' if not\n            passed.\n    \"\"\"\n\n    webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n    if not webserver_url:\n        msg = \"Couldn't find webserver url\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    rest_api_url = \"{}/timers_manager/start_timer\".format(webserver_url)\n    try:\n        import requests\n    except Exception:\n        msg = \"Couldn't start timer ('requests' is not available)\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n    data = {\n        \"project_name\": project_name,\n        \"folder_path\": folder_path,\n        \"task_name\": task_name\n    }\n\n    return requests.post(rest_api_url, json=data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.stop_timer_with_webserver","title":"<code>stop_timer_with_webserver(logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepared method for calling stop timers on REST api.</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger</code> <p>Logger used for logging messages.</p> <code>None</code> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef stop_timer_with_webserver(logger=None):\n    \"\"\"Prepared method for calling stop timers on REST api.\n\n    Args:\n        logger (logging.Logger): Logger used for logging messages.\n    \"\"\"\n\n    webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n    if not webserver_url:\n        msg = \"Couldn't find webserver url\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    rest_api_url = \"{}/timers_manager/stop_timer\".format(webserver_url)\n    try:\n        import requests\n    except Exception:\n        msg = \"Couldn't start timer ('requests' is not available)\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    return requests.post(rest_api_url)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.stop_timers","title":"<code>stop_timers()</code>","text":"<p>Stop all timers.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def stop_timers(self):\n    \"\"\"Stop all timers.\"\"\"\n    if self.is_running is False:\n        return\n\n    if self._widget_user_idle is not None:\n        self._widget_user_idle.set_timer_stopped()\n    self.is_running = False\n\n    self.timer_stopped(None)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.timer_started","title":"<code>timer_started(source_id, data)</code>","text":"<p>Connector triggered that timer has started.</p> <p>New timer has started for context in data.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def timer_started(self, source_id, data):\n    \"\"\"Connector triggered that timer has started.\n\n    New timer has started for context in data.\n    \"\"\"\n    for module_id, connector in self._connectors_by_module_id.items():\n        if module_id == source_id:\n            continue\n\n        try:\n            connector.start_timer(data)\n        except Exception:\n            self.log.info(\n                \"Failed to start timer on connector {}\".format(\n                    str(connector)\n                )\n            )\n\n    self.last_task = data\n    self.is_running = True\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.timer_stopped","title":"<code>timer_stopped(source_id)</code>","text":"<p>Connector triggered that hist timer has stopped.</p> <p>Should stop all other timers.</p> <p>TODO: - pass context for which timer has stopped to validate if timers are     same and valid</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def timer_stopped(self, source_id):\n    \"\"\"Connector triggered that hist timer has stopped.\n\n    Should stop all other timers.\n\n    TODO:\n    - pass context for which timer has stopped to validate if timers are\n        same and valid\n    \"\"\"\n    for module_id, connector in self._connectors_by_module_id.items():\n        if module_id == source_id:\n            continue\n\n        try:\n            connector.stop_timer()\n        except Exception:\n            self.log.info(\n                \"Failed to stop timer on connector {}\".format(\n                    str(connector)\n                )\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/index.html#client.ayon_timers_manager.TimersManager.webserver_initialization","title":"<code>webserver_initialization(server_manager)</code>","text":"<p>Add routes for timers to be able start/stop with rest api.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def webserver_initialization(self, server_manager):\n    \"\"\"Add routes for timers to be able start/stop with rest api.\"\"\"\n    if self.tray_initialized:\n        from .rest_api import TimersManagerModuleRestApi\n        self.rest_api_obj = TimersManagerModuleRestApi(\n            self, server_manager\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/exceptions.html","title":"exceptions","text":""},{"location":"autoapi/client/ayon_timers_manager/exceptions.html#client.ayon_timers_manager.exceptions.InvalidContextError","title":"<code>InvalidContextError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Context for which the timer should be started is invalid.</p> Source code in <code>client/ayon_timers_manager/exceptions.py</code> <pre><code>class InvalidContextError(ValueError):\n    \"\"\"Context for which the timer should be started is invalid.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html","title":"idle_threads","text":""},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html#client.ayon_timers_manager.idle_threads.IdleItem","title":"<code>IdleItem</code>","text":"<p>Python object holds information if state of idle changed.</p> <p>This item is used to be independent from Qt objects.</p> Source code in <code>client/ayon_timers_manager/idle_threads.py</code> <pre><code>class IdleItem:\n    \"\"\"Python object holds information if state of idle changed.\n\n    This item is used to be independent from Qt objects.\n    \"\"\"\n    def __init__(self):\n        self.changed = False\n\n    def reset(self):\n        self.changed = False\n\n    def set_changed(self, changed=True):\n        self.changed = changed\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html#client.ayon_timers_manager.idle_threads.IdleManager","title":"<code>IdleManager</code>","text":"<p>               Bases: <code>QThread</code></p> <p>Measure user's idle time in seconds. Idle time resets on keyboard/mouse input. Is able to emit signals at specific time idle.</p> Source code in <code>client/ayon_timers_manager/idle_threads.py</code> <pre><code>class IdleManager(QtCore.QThread):\n    \"\"\" Measure user's idle time in seconds.\n    Idle time resets on keyboard/mouse input.\n    Is able to emit signals at specific time idle.\n    \"\"\"\n    time_signals = {}\n    idle_time = 0\n    signal_reset_timer = QtCore.Signal()\n\n    def __init__(self):\n        super(IdleManager, self).__init__()\n        self.log = Logger.get_logger(self.__class__.__name__)\n        self.signal_reset_timer.connect(self._reset_time)\n\n        self.idle_item = IdleItem()\n\n        self._is_running = False\n        self._mouse_thread = None\n        self._keyboard_thread = None\n\n    def add_time_signal(self, emit_time, signal):\n        \"\"\" If any module want to use IdleManager, need to use add_time_signal\n\n        Args:\n            emit_time(int): Time when signal will be emitted.\n            signal(QtCore.Signal): Signal that will be emitted\n                (without objects).\n        \"\"\"\n        if emit_time not in self.time_signals:\n            self.time_signals[emit_time] = []\n        self.time_signals[emit_time].append(signal)\n\n    @property\n    def is_running(self):\n        return self._is_running\n\n    def _reset_time(self):\n        self.idle_time = 0\n\n    def stop(self):\n        self._is_running = False\n\n    def _on_mouse_destroy(self):\n        self._mouse_thread = None\n\n    def _on_keyboard_destroy(self):\n        self._keyboard_thread = None\n\n    def run(self):\n        self.log.info('IdleManager has started')\n        self._is_running = True\n\n        thread_mouse = MouseThread(self.idle_item)\n        thread_keyboard = KeyboardThread(self.idle_item)\n\n        thread_mouse.destroyed.connect(self._on_mouse_destroy)\n        thread_keyboard.destroyed.connect(self._on_keyboard_destroy)\n\n        self._mouse_thread = thread_mouse\n        self._keyboard_thread = thread_keyboard\n\n        thread_mouse.start()\n        thread_keyboard.start()\n\n        # Main loop here is each second checked if idle item changed state\n        while self._is_running:\n            if self.idle_item.changed:\n                self.idle_item.reset()\n                self.signal_reset_timer.emit()\n            else:\n                self.idle_time += 1\n\n            if self.idle_time in self.time_signals:\n                for signal in self.time_signals[self.idle_time]:\n                    signal.emit()\n            time.sleep(1)\n\n        self._post_run()\n        self.log.info('IdleManager has stopped')\n\n    def _post_run(self):\n        # Stop threads if still exist\n        if self._mouse_thread is not None:\n            self._mouse_thread.signal_stop.emit()\n            self._mouse_thread.terminate()\n            self._mouse_thread.wait()\n\n        if self._keyboard_thread is not None:\n            self._keyboard_thread.signal_stop.emit()\n            self._keyboard_thread.terminate()\n            self._keyboard_thread.wait()\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html#client.ayon_timers_manager.idle_threads.IdleManager.add_time_signal","title":"<code>add_time_signal(emit_time, signal)</code>","text":"<p>If any module want to use IdleManager, need to use add_time_signal</p> <p>Parameters:</p> Name Type Description Default <code>emit_time</code> <code>int</code> <p>Time when signal will be emitted.</p> required <code>signal</code> <code>Signal</code> <p>Signal that will be emitted (without objects).</p> required Source code in <code>client/ayon_timers_manager/idle_threads.py</code> <pre><code>def add_time_signal(self, emit_time, signal):\n    \"\"\" If any module want to use IdleManager, need to use add_time_signal\n\n    Args:\n        emit_time(int): Time when signal will be emitted.\n        signal(QtCore.Signal): Signal that will be emitted\n            (without objects).\n    \"\"\"\n    if emit_time not in self.time_signals:\n        self.time_signals[emit_time] = []\n    self.time_signals[emit_time].append(signal)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html#client.ayon_timers_manager.idle_threads.KeyboardThread","title":"<code>KeyboardThread</code>","text":"<p>               Bases: <code>QThread</code></p> <p>Listens user's keyboard input</p> Source code in <code>client/ayon_timers_manager/idle_threads.py</code> <pre><code>class KeyboardThread(QtCore.QThread):\n    \"\"\"Listens user's keyboard input\n    \"\"\"\n    signal_stop = QtCore.Signal()\n\n    def __init__(self, idle_item):\n        super(KeyboardThread, self).__init__()\n        self.signal_stop.connect(self.stop)\n        self.k_listener = None\n        self.idle_item = idle_item\n\n    def stop(self):\n        if self.k_listener is not None:\n            listener = self.k_listener\n            self.k_listener = None\n            listener.stop()\n\n    def on_press(self, *args, **kwargs):\n        self.idle_item.set_changed()\n\n    def run(self):\n        self.k_listener = keyboard.Listener(on_press=self.on_press)\n        self.k_listener.start()\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/idle_threads.html#client.ayon_timers_manager.idle_threads.MouseThread","title":"<code>MouseThread</code>","text":"<p>               Bases: <code>QThread</code></p> <p>Listens user's mouse movement.</p> Source code in <code>client/ayon_timers_manager/idle_threads.py</code> <pre><code>class MouseThread(QtCore.QThread):\n    \"\"\"Listens user's mouse movement.\"\"\"\n    signal_stop = QtCore.Signal()\n\n    def __init__(self, idle_item):\n        super(MouseThread, self).__init__()\n        self.signal_stop.connect(self.stop)\n        self.m_listener = None\n        self.idle_item = idle_item\n\n    def stop(self):\n        if self.m_listener is not None:\n            self.m_listener.stop()\n\n    def on_move(self, *args, **kwargs):\n        self.idle_item.set_changed()\n\n    def run(self):\n        self.m_listener = mouse.Listener(on_move=self.on_move)\n        self.m_listener.start()\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/rest_api.html","title":"rest_api","text":""},{"location":"autoapi/client/ayon_timers_manager/rest_api.html#client.ayon_timers_manager.rest_api.TimersManagerModuleRestApi","title":"<code>TimersManagerModuleRestApi</code>","text":"<p>REST API endpoint used for calling from hosts when context change happens in Workfile app.</p> Source code in <code>client/ayon_timers_manager/rest_api.py</code> <pre><code>class TimersManagerModuleRestApi:\n    \"\"\"\n        REST API endpoint used for calling from hosts when context change\n        happens in Workfile app.\n    \"\"\"\n    def __init__(self, user_module, server_manager):\n        self._log = None\n        self.module = user_module\n        self.server_manager = server_manager\n\n        self.prefix = \"/timers_manager\"\n\n        self.register()\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def register(self):\n        self.server_manager.add_route(\n            \"POST\",\n            self.prefix + \"/start_timer\",\n            self.start_timer\n        )\n        self.server_manager.add_route(\n            \"POST\",\n            self.prefix + \"/stop_timer\",\n            self.stop_timer\n        )\n        self.server_manager.add_route(\n            \"GET\",\n            self.prefix + \"/get_task_time\",\n            self.get_task_time\n        )\n\n    async def start_timer(self, request):\n        data = await request.json()\n        try:\n            project_name = data[\"project_name\"]\n            folder_path = data[\"folder_path\"]\n            task_name = data[\"task_name\"]\n        except KeyError:\n            msg = (\n                \"Payload must contain fields 'project_name,\"\n                \" 'folder_path' and 'task_name'\"\n            )\n            self.log.error(msg)\n            return Response(status=400, message=msg)\n\n        self.module.stop_timers()\n        try:\n            self.module.start_timer(project_name, folder_path, task_name)\n        except Exception as exc:\n            return Response(status=404, message=str(exc))\n\n        return Response(status=200)\n\n    async def stop_timer(self, request):\n        self.module.stop_timers()\n        return Response(status=200)\n\n    async def get_task_time(self, request):\n        data = await request.json()\n        try:\n            project_name = data[\"project_name\"]\n            folder_path = data[\"folder_path\"]\n            task_name = data[\"task_name\"]\n        except KeyError:\n            message = (\n                \"Payload must contain fields 'project_name, 'folder_path',\"\n                \" 'task_name'\"\n            )\n            self.log.warning(message)\n            return Response(text=message, status=404)\n\n        time = self.module.get_task_time(project_name, folder_path, task_name)\n        return Response(text=json.dumps(time))\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html","title":"timers_manager","text":""},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.ExampleTimersManagerConnector","title":"<code>ExampleTimersManagerConnector</code>","text":"<p>Timers manager can handle timers of multiple modules/addons.</p> <p>Module must have object under <code>timers_manager_connector</code> attribute with few methods. This is example class of the object that could be stored under module.</p> <p>Required methods are 'stop_timer' and 'start_timer'.</p> <p>Example of <code>data</code> that are passed during changing timer:</p> <pre><code>data = {\n    \"project_name\": project_name,\n    \"folder_id\": folder_id,\n    \"folder_path\": folder_entity[\"path\"],\n    \"task_name\": task_name,\n    \"task_type\": task_type,\n    # Deprecated\n    \"asset_id\": folder_id,\n    \"asset_name\": folder_entity[\"name\"],\n    \"hierarchy\": hierarchy_items,\n}\n</code></pre> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>class ExampleTimersManagerConnector:\n    \"\"\"Timers manager can handle timers of multiple modules/addons.\n\n    Module must have object under `timers_manager_connector` attribute with\n    few methods. This is example class of the object that could be stored under\n    module.\n\n    Required methods are 'stop_timer' and 'start_timer'.\n\n    Example of `data` that are passed during changing timer:\n    ```\n    data = {\n        \"project_name\": project_name,\n        \"folder_id\": folder_id,\n        \"folder_path\": folder_entity[\"path\"],\n        \"task_name\": task_name,\n        \"task_type\": task_type,\n        # Deprecated\n        \"asset_id\": folder_id,\n        \"asset_name\": folder_entity[\"name\"],\n        \"hierarchy\": hierarchy_items,\n    }\n    ```\n    \"\"\"\n\n    # Not needed at all\n    def __init__(self, module):\n        # Store timer manager module to be able call it's methods when needed\n        self._timers_manager_module = None\n\n        # Store module which want to use timers manager to have access\n        self._module = module\n\n    # Required\n    def stop_timer(self):\n        \"\"\"Called by timers manager when module should stop timer.\"\"\"\n        self._module.stop_timer()\n\n    # Required\n    def start_timer(self, data):\n        \"\"\"Method called by timers manager when should start timer.\"\"\"\n        self._module.start_timer(data)\n\n    # Optional\n    def register_timers_manager(self, timer_manager_module):\n        \"\"\"Method called by timers manager where it's object is passed.\n\n        This is moment when timers manager module can be store to be able\n        call it's callbacks (e.g. timer started).\n        \"\"\"\n        self._timers_manager_module = timer_manager_module\n\n    # Custom implementation\n    def timer_started(self, data):\n        \"\"\"This is example of possibility to trigger callbacks on manager.\"\"\"\n        if self._timers_manager_module is not None:\n            self._timers_manager_module.timer_started(self._module.id, data)\n\n    # Custom implementation\n    def timer_stopped(self):\n        if self._timers_manager_module is not None:\n            self._timers_manager_module.timer_stopped(self._module.id)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.ExampleTimersManagerConnector.register_timers_manager","title":"<code>register_timers_manager(timer_manager_module)</code>","text":"<p>Method called by timers manager where it's object is passed.</p> <p>This is moment when timers manager module can be store to be able call it's callbacks (e.g. timer started).</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def register_timers_manager(self, timer_manager_module):\n    \"\"\"Method called by timers manager where it's object is passed.\n\n    This is moment when timers manager module can be store to be able\n    call it's callbacks (e.g. timer started).\n    \"\"\"\n    self._timers_manager_module = timer_manager_module\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.ExampleTimersManagerConnector.start_timer","title":"<code>start_timer(data)</code>","text":"<p>Method called by timers manager when should start timer.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def start_timer(self, data):\n    \"\"\"Method called by timers manager when should start timer.\"\"\"\n    self._module.start_timer(data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.ExampleTimersManagerConnector.stop_timer","title":"<code>stop_timer()</code>","text":"<p>Called by timers manager when module should stop timer.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def stop_timer(self):\n    \"\"\"Called by timers manager when module should stop timer.\"\"\"\n    self._module.stop_timer()\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.ExampleTimersManagerConnector.timer_started","title":"<code>timer_started(data)</code>","text":"<p>This is example of possibility to trigger callbacks on manager.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def timer_started(self, data):\n    \"\"\"This is example of possibility to trigger callbacks on manager.\"\"\"\n    if self._timers_manager_module is not None:\n        self._timers_manager_module.timer_started(self._module.id, data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager","title":"<code>TimersManager</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>ITrayService</code>, <code>IPluginPaths</code></p> <p>Handles about Timers.</p> <p>Should be able to start/stop all timers at once.</p> <p>To be able use this advantage module has to have attribute with name <code>timers_manager_connector</code> which has two methods 'stop_timer' and 'start_timer'. Optionally may have <code>register_timers_manager</code> where object of TimersManager module is passed to be able call it's callbacks.</p> <p>See <code>ExampleTimersManagerConnector</code>.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>class TimersManager(\n    AYONAddon,\n    ITrayService,\n    IPluginPaths\n):\n    \"\"\" Handles about Timers.\n\n    Should be able to start/stop all timers at once.\n\n    To be able use this advantage module has to have attribute with name\n    `timers_manager_connector` which has two methods 'stop_timer'\n    and 'start_timer'. Optionally may have `register_timers_manager` where\n    object of TimersManager module is passed to be able call it's callbacks.\n\n    See `ExampleTimersManagerConnector`.\n    \"\"\"\n    name = \"timers_manager\"\n    version = __version__\n    label = \"Timers Service\"\n\n    _required_methods = (\n        \"stop_timer\",\n        \"start_timer\"\n    )\n\n    def initialize(self, studio_settings):\n        timers_settings = studio_settings.get(self.name)\n        enabled = timers_settings is not None\n\n        auto_stop = False\n        full_time = 0\n        message_time = 0\n        if enabled:\n            # When timer will stop if idle manager is running (minutes)\n            full_time = int(timers_settings[\"full_time\"] * 60)\n            # How many minutes before the timer is stopped will popup the message\n            message_time = int(timers_settings[\"message_time\"] * 60)\n\n            auto_stop = timers_settings[\"auto_stop\"]\n            platform_name = platform.system().lower()\n            # Turn of auto stop on MacOs because pynput requires root permissions\n            #    and on linux can cause thread locks on application close\n            if full_time &lt;= 0 or platform_name in (\"darwin\", \"linux\"):\n                auto_stop = False\n\n        self.enabled = enabled\n        self.auto_stop = auto_stop\n        self.time_show_message = full_time - message_time\n        self.time_stop_timer = full_time\n\n        self.is_running = False\n        self.last_task = None\n\n        # Tray attributes\n        self._signal_handler = None\n        self._widget_user_idle = None\n        self._idle_manager = None\n\n        self._connectors_by_module_id = {}\n        self._modules_by_id = {}\n\n    def tray_init(self):\n        if not self.auto_stop:\n            return\n\n        from .idle_threads import IdleManager\n        from .widget_user_idle import WidgetUserIdle, SignalHandler\n\n        signal_handler = SignalHandler(self)\n        idle_manager = IdleManager()\n        widget_user_idle = WidgetUserIdle(self)\n        widget_user_idle.set_countdown_start(\n            self.time_stop_timer - self.time_show_message\n        )\n\n        idle_manager.signal_reset_timer.connect(\n            widget_user_idle.reset_countdown\n        )\n        idle_manager.add_time_signal(\n            self.time_show_message, signal_handler.signal_show_message\n        )\n        idle_manager.add_time_signal(\n            self.time_stop_timer, signal_handler.signal_stop_timers\n        )\n\n        self._signal_handler = signal_handler\n        self._widget_user_idle = widget_user_idle\n        self._idle_manager = idle_manager\n\n    def tray_start(self, *_a, **_kw):\n        if self._idle_manager:\n            self._idle_manager.start()\n\n    def tray_exit(self):\n        if self._idle_manager:\n            self._idle_manager.stop()\n            self._idle_manager.wait()\n\n    def get_timer_data_for_path(self, task_path):\n        \"\"\"Convert string path to a timer data.\n\n        It is expected that first item is project name, last item is task name\n        and folder path in the middle.\n        \"\"\"\n        path_items = task_path.split(\"/\")\n        task_name = path_items.pop(-1)\n        project_name = path_items.pop(0)\n        folder_path = \"/\" + \"/\".join(path_items)\n        return self.get_timer_data_for_context(\n            project_name, folder_path, task_name, self.log\n        )\n\n    def get_launch_hook_paths(self):\n        \"\"\"Implementation for applications launch hooks.\"\"\"\n\n        return [\n            os.path.join(TIMER_MODULE_DIR, \"launch_hooks\")\n        ]\n\n    def get_plugin_paths(self):\n        \"\"\"Implementation of `IPluginPaths`.\"\"\"\n\n        return {\n            \"publish\": [os.path.join(TIMER_MODULE_DIR, \"plugins\", \"publish\")]\n        }\n\n    @staticmethod\n    def get_timer_data_for_context(\n        project_name, folder_path, task_name, logger=None\n    ):\n        \"\"\"Prepare data for timer related callbacks.\"\"\"\n        if not project_name or not folder_path or not task_name:\n            raise InvalidContextError((\n                \"Missing context information got\"\n                \" Project: \\\"{}\\\" Folder: \\\"{}\\\" Task: \\\"{}\\\"\"\n            ).format(str(project_name), str(folder_path), str(task_name)))\n\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name,\n            folder_path,\n            fields={\"id\", \"name\", \"path\"}\n        )\n\n        if not folder_entity:\n            raise InvalidContextError((\n                \"Folder \\\"{}\\\" not found in project \\\"{}\\\"\"\n            ).format(folder_path, project_name))\n\n        folder_id = folder_entity[\"id\"]\n        task_entity = ayon_api.get_task_by_name(\n            project_name, folder_id, task_name\n        )\n        if not task_entity:\n            raise InvalidContextError((\n                \"Task \\\"{}\\\" not found on folder \\\"{}\\\" in project \\\"{}\\\"\"\n            ).format(task_name, folder_path, project_name))\n\n        task_type = \"\"\n        try:\n            task_type = task_entity[\"taskType\"]\n        except KeyError:\n            msg = \"Couldn't find task_type for {}\".format(task_name)\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n\n        hierarchy_items = folder_entity[\"path\"].split(\"/\")\n        hierarchy_items.pop(0)\n\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"folder_path\": folder_entity[\"path\"],\n            \"task_name\": task_name,\n            \"task_type\": task_type,\n            \"asset_id\": folder_id,\n            \"asset_name\": folder_entity[\"name\"],\n            \"hierarchy\": hierarchy_items,\n        }\n\n    def start_timer(self, project_name, folder_path, task_name):\n        \"\"\"Start timer for passed context.\n\n        Args:\n            project_name (str): Project name.\n            folder_path (str): Folder path.\n            task_name (str): Task name.\n        \"\"\"\n        data = self.get_timer_data_for_context(\n            project_name, folder_path, task_name, self.log\n        )\n        self.timer_started(None, data)\n\n    def get_task_time(self, project_name, folder_path, task_name):\n        \"\"\"Get total time for passed context.\n\n        TODO:\n        - convert context to timer data\n        \"\"\"\n        times = {}\n        for module_id, connector in self._connectors_by_module_id.items():\n            if hasattr(connector, \"get_task_time\"):\n                module = self._modules_by_id[module_id]\n                times[module.name] = connector.get_task_time(\n                    project_name, folder_path, task_name\n                )\n        return times\n\n    def timer_started(self, source_id, data):\n        \"\"\"Connector triggered that timer has started.\n\n        New timer has started for context in data.\n        \"\"\"\n        for module_id, connector in self._connectors_by_module_id.items():\n            if module_id == source_id:\n                continue\n\n            try:\n                connector.start_timer(data)\n            except Exception:\n                self.log.info(\n                    \"Failed to start timer on connector {}\".format(\n                        str(connector)\n                    )\n                )\n\n        self.last_task = data\n        self.is_running = True\n\n    def timer_stopped(self, source_id):\n        \"\"\"Connector triggered that hist timer has stopped.\n\n        Should stop all other timers.\n\n        TODO:\n        - pass context for which timer has stopped to validate if timers are\n            same and valid\n        \"\"\"\n        for module_id, connector in self._connectors_by_module_id.items():\n            if module_id == source_id:\n                continue\n\n            try:\n                connector.stop_timer()\n            except Exception:\n                self.log.info(\n                    \"Failed to stop timer on connector {}\".format(\n                        str(connector)\n                    )\n                )\n\n    def restart_timers(self):\n        if self.last_task is not None:\n            self.timer_started(None, self.last_task)\n\n    def stop_timers(self):\n        \"\"\"Stop all timers.\"\"\"\n        if self.is_running is False:\n            return\n\n        if self._widget_user_idle is not None:\n            self._widget_user_idle.set_timer_stopped()\n        self.is_running = False\n\n        self.timer_stopped(None)\n\n    def connect_with_addons(self, enabled_modules):\n        for module in enabled_modules:\n            connector = getattr(module, \"timers_manager_connector\", None)\n            if connector is None:\n                continue\n\n            missing_methods = set()\n            for method_name in self._required_methods:\n                if not hasattr(connector, method_name):\n                    missing_methods.add(method_name)\n\n            if missing_methods:\n                joined = \", \".join(\n                    ['\"{}\"'.format(name for name in missing_methods)]\n                )\n                self.log.info((\n                    \"Module \\\"{}\\\" has missing required methods {}.\"\n                ).format(module.name, joined))\n                continue\n\n            self._connectors_by_module_id[module.id] = connector\n            self._modules_by_id[module.id] = module\n\n            # Optional method\n            if hasattr(connector, \"register_timers_manager\"):\n                try:\n                    connector.register_timers_manager(self)\n                except Exception:\n                    self.log.info((\n                        \"Failed to register timers manager\"\n                        \" for connector of module \\\"{}\\\".\"\n                    ).format(module.name))\n\n    def show_message(self):\n        if self.is_running is False:\n            return\n        if not self._widget_user_idle.is_showed():\n            self._widget_user_idle.reset_countdown()\n            self._widget_user_idle.show()\n\n    # Webserver module implementation\n    def webserver_initialization(self, server_manager):\n        \"\"\"Add routes for timers to be able start/stop with rest api.\"\"\"\n        if self.tray_initialized:\n            from .rest_api import TimersManagerModuleRestApi\n            self.rest_api_obj = TimersManagerModuleRestApi(\n                self, server_manager\n            )\n\n    @staticmethod\n    def start_timer_with_webserver(\n        project_name, folder_path, task_name, logger=None\n    ):\n        \"\"\"Prepared method for calling change timers on REST api.\n\n        Webserver must be active. At the moment is Webserver running only when\n        OpenPype Tray is used.\n\n        Args:\n            project_name (str): Project name.\n            folder_path (str): Folder path.\n            task_name (str): Task name.\n            logger (logging.Logger): Logger object. Using 'print' if not\n                passed.\n        \"\"\"\n\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n        if not webserver_url:\n            msg = \"Couldn't find webserver url\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        rest_api_url = \"{}/timers_manager/start_timer\".format(webserver_url)\n        try:\n            import requests\n        except Exception:\n            msg = \"Couldn't start timer ('requests' is not available)\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n        data = {\n            \"project_name\": project_name,\n            \"folder_path\": folder_path,\n            \"task_name\": task_name\n        }\n\n        return requests.post(rest_api_url, json=data)\n\n    @staticmethod\n    def stop_timer_with_webserver(logger=None):\n        \"\"\"Prepared method for calling stop timers on REST api.\n\n        Args:\n            logger (logging.Logger): Logger used for logging messages.\n        \"\"\"\n\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n        if not webserver_url:\n            msg = \"Couldn't find webserver url\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        rest_api_url = \"{}/timers_manager/stop_timer\".format(webserver_url)\n        try:\n            import requests\n        except Exception:\n            msg = \"Couldn't start timer ('requests' is not available)\"\n            if logger is not None:\n                logger.warning(msg)\n            else:\n                print(msg)\n            return\n\n        return requests.post(rest_api_url)\n\n    def on_host_install(self, host, host_name, project_name):\n        self.log.debug(\"Installing task changed callback\")\n        register_event_callback(\"taskChanged\", self._on_host_task_change)\n\n    def _on_host_task_change(self, event):\n        project_name = event[\"project_name\"]\n        folder_path = event[\"folder_path\"]\n        task_name = event[\"task_name\"]\n        self.log.debug((\n            \"Sending message that timer should change to\"\n            \" Project: {} Folder: {} Task: {}\"\n        ).format(project_name, folder_path, task_name))\n\n        self.start_timer_with_webserver(\n            project_name, folder_path, task_name, self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.get_launch_hook_paths","title":"<code>get_launch_hook_paths()</code>","text":"<p>Implementation for applications launch hooks.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_launch_hook_paths(self):\n    \"\"\"Implementation for applications launch hooks.\"\"\"\n\n    return [\n        os.path.join(TIMER_MODULE_DIR, \"launch_hooks\")\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.get_plugin_paths","title":"<code>get_plugin_paths()</code>","text":"<p>Implementation of <code>IPluginPaths</code>.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_plugin_paths(self):\n    \"\"\"Implementation of `IPluginPaths`.\"\"\"\n\n    return {\n        \"publish\": [os.path.join(TIMER_MODULE_DIR, \"plugins\", \"publish\")]\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.get_task_time","title":"<code>get_task_time(project_name, folder_path, task_name)</code>","text":"<p>Get total time for passed context.</p> <p>TODO: - convert context to timer data</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_task_time(self, project_name, folder_path, task_name):\n    \"\"\"Get total time for passed context.\n\n    TODO:\n    - convert context to timer data\n    \"\"\"\n    times = {}\n    for module_id, connector in self._connectors_by_module_id.items():\n        if hasattr(connector, \"get_task_time\"):\n            module = self._modules_by_id[module_id]\n            times[module.name] = connector.get_task_time(\n                project_name, folder_path, task_name\n            )\n    return times\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.get_timer_data_for_context","title":"<code>get_timer_data_for_context(project_name, folder_path, task_name, logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepare data for timer related callbacks.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef get_timer_data_for_context(\n    project_name, folder_path, task_name, logger=None\n):\n    \"\"\"Prepare data for timer related callbacks.\"\"\"\n    if not project_name or not folder_path or not task_name:\n        raise InvalidContextError((\n            \"Missing context information got\"\n            \" Project: \\\"{}\\\" Folder: \\\"{}\\\" Task: \\\"{}\\\"\"\n        ).format(str(project_name), str(folder_path), str(task_name)))\n\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name,\n        folder_path,\n        fields={\"id\", \"name\", \"path\"}\n    )\n\n    if not folder_entity:\n        raise InvalidContextError((\n            \"Folder \\\"{}\\\" not found in project \\\"{}\\\"\"\n        ).format(folder_path, project_name))\n\n    folder_id = folder_entity[\"id\"]\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_id, task_name\n    )\n    if not task_entity:\n        raise InvalidContextError((\n            \"Task \\\"{}\\\" not found on folder \\\"{}\\\" in project \\\"{}\\\"\"\n        ).format(task_name, folder_path, project_name))\n\n    task_type = \"\"\n    try:\n        task_type = task_entity[\"taskType\"]\n    except KeyError:\n        msg = \"Couldn't find task_type for {}\".format(task_name)\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n\n    hierarchy_items = folder_entity[\"path\"].split(\"/\")\n    hierarchy_items.pop(0)\n\n    return {\n        \"project_name\": project_name,\n        \"folder_id\": folder_id,\n        \"folder_path\": folder_entity[\"path\"],\n        \"task_name\": task_name,\n        \"task_type\": task_type,\n        \"asset_id\": folder_id,\n        \"asset_name\": folder_entity[\"name\"],\n        \"hierarchy\": hierarchy_items,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.get_timer_data_for_path","title":"<code>get_timer_data_for_path(task_path)</code>","text":"<p>Convert string path to a timer data.</p> <p>It is expected that first item is project name, last item is task name and folder path in the middle.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def get_timer_data_for_path(self, task_path):\n    \"\"\"Convert string path to a timer data.\n\n    It is expected that first item is project name, last item is task name\n    and folder path in the middle.\n    \"\"\"\n    path_items = task_path.split(\"/\")\n    task_name = path_items.pop(-1)\n    project_name = path_items.pop(0)\n    folder_path = \"/\" + \"/\".join(path_items)\n    return self.get_timer_data_for_context(\n        project_name, folder_path, task_name, self.log\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.start_timer","title":"<code>start_timer(project_name, folder_path, task_name)</code>","text":"<p>Start timer for passed context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def start_timer(self, project_name, folder_path, task_name):\n    \"\"\"Start timer for passed context.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n    \"\"\"\n    data = self.get_timer_data_for_context(\n        project_name, folder_path, task_name, self.log\n    )\n    self.timer_started(None, data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.start_timer_with_webserver","title":"<code>start_timer_with_webserver(project_name, folder_path, task_name, logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepared method for calling change timers on REST api.</p> <p>Webserver must be active. At the moment is Webserver running only when OpenPype Tray is used.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>logger</code> <code>Logger</code> <p>Logger object. Using 'print' if not passed.</p> <code>None</code> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef start_timer_with_webserver(\n    project_name, folder_path, task_name, logger=None\n):\n    \"\"\"Prepared method for calling change timers on REST api.\n\n    Webserver must be active. At the moment is Webserver running only when\n    OpenPype Tray is used.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        logger (logging.Logger): Logger object. Using 'print' if not\n            passed.\n    \"\"\"\n\n    webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n    if not webserver_url:\n        msg = \"Couldn't find webserver url\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    rest_api_url = \"{}/timers_manager/start_timer\".format(webserver_url)\n    try:\n        import requests\n    except Exception:\n        msg = \"Couldn't start timer ('requests' is not available)\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n    data = {\n        \"project_name\": project_name,\n        \"folder_path\": folder_path,\n        \"task_name\": task_name\n    }\n\n    return requests.post(rest_api_url, json=data)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.stop_timer_with_webserver","title":"<code>stop_timer_with_webserver(logger=None)</code>  <code>staticmethod</code>","text":"<p>Prepared method for calling stop timers on REST api.</p> <p>Parameters:</p> Name Type Description Default <code>logger</code> <code>Logger</code> <p>Logger used for logging messages.</p> <code>None</code> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>@staticmethod\ndef stop_timer_with_webserver(logger=None):\n    \"\"\"Prepared method for calling stop timers on REST api.\n\n    Args:\n        logger (logging.Logger): Logger used for logging messages.\n    \"\"\"\n\n    webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n    if not webserver_url:\n        msg = \"Couldn't find webserver url\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    rest_api_url = \"{}/timers_manager/stop_timer\".format(webserver_url)\n    try:\n        import requests\n    except Exception:\n        msg = \"Couldn't start timer ('requests' is not available)\"\n        if logger is not None:\n            logger.warning(msg)\n        else:\n            print(msg)\n        return\n\n    return requests.post(rest_api_url)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.stop_timers","title":"<code>stop_timers()</code>","text":"<p>Stop all timers.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def stop_timers(self):\n    \"\"\"Stop all timers.\"\"\"\n    if self.is_running is False:\n        return\n\n    if self._widget_user_idle is not None:\n        self._widget_user_idle.set_timer_stopped()\n    self.is_running = False\n\n    self.timer_stopped(None)\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.timer_started","title":"<code>timer_started(source_id, data)</code>","text":"<p>Connector triggered that timer has started.</p> <p>New timer has started for context in data.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def timer_started(self, source_id, data):\n    \"\"\"Connector triggered that timer has started.\n\n    New timer has started for context in data.\n    \"\"\"\n    for module_id, connector in self._connectors_by_module_id.items():\n        if module_id == source_id:\n            continue\n\n        try:\n            connector.start_timer(data)\n        except Exception:\n            self.log.info(\n                \"Failed to start timer on connector {}\".format(\n                    str(connector)\n                )\n            )\n\n    self.last_task = data\n    self.is_running = True\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.timer_stopped","title":"<code>timer_stopped(source_id)</code>","text":"<p>Connector triggered that hist timer has stopped.</p> <p>Should stop all other timers.</p> <p>TODO: - pass context for which timer has stopped to validate if timers are     same and valid</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def timer_stopped(self, source_id):\n    \"\"\"Connector triggered that hist timer has stopped.\n\n    Should stop all other timers.\n\n    TODO:\n    - pass context for which timer has stopped to validate if timers are\n        same and valid\n    \"\"\"\n    for module_id, connector in self._connectors_by_module_id.items():\n        if module_id == source_id:\n            continue\n\n        try:\n            connector.stop_timer()\n        except Exception:\n            self.log.info(\n                \"Failed to stop timer on connector {}\".format(\n                    str(connector)\n                )\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/timers_manager.html#client.ayon_timers_manager.timers_manager.TimersManager.webserver_initialization","title":"<code>webserver_initialization(server_manager)</code>","text":"<p>Add routes for timers to be able start/stop with rest api.</p> Source code in <code>client/ayon_timers_manager/timers_manager.py</code> <pre><code>def webserver_initialization(self, server_manager):\n    \"\"\"Add routes for timers to be able start/stop with rest api.\"\"\"\n    if self.tray_initialized:\n        from .rest_api import TimersManagerModuleRestApi\n        self.rest_api_obj = TimersManagerModuleRestApi(\n            self, server_manager\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/version.html","title":"version","text":"<p>Package declaring AYON addon 'timers_manager' version.</p>"},{"location":"autoapi/client/ayon_timers_manager/widget_user_idle.html","title":"widget_user_idle","text":""},{"location":"autoapi/client/ayon_timers_manager/launch_hooks/index.html","title":"launch_hooks","text":""},{"location":"autoapi/client/ayon_timers_manager/launch_hooks/post_start_timer.html","title":"post_start_timer","text":""},{"location":"autoapi/client/ayon_timers_manager/launch_hooks/post_start_timer.html#client.ayon_timers_manager.launch_hooks.post_start_timer.PostStartTimerHook","title":"<code>PostStartTimerHook</code>","text":"<p>               Bases: <code>PostLaunchHook</code></p> <p>Start timer with TimersManager module.</p> <p>This module requires enabled TimerManager module.</p> Source code in <code>client/ayon_timers_manager/launch_hooks/post_start_timer.py</code> <pre><code>class PostStartTimerHook(PostLaunchHook):\n    \"\"\"Start timer with TimersManager module.\n\n    This module requires enabled TimerManager module.\n    \"\"\"\n    order = None\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        project_name = self.data.get(\"project_name\")\n        folder_path = self.data.get(\"folder_path\")\n        task_name = self.data.get(\"task_name\")\n\n        missing_context_keys = set()\n        if not project_name:\n            missing_context_keys.add(\"project_name\")\n        if not folder_path:\n            missing_context_keys.add(\"folder_path\")\n        if not task_name:\n            missing_context_keys.add(\"task_name\")\n\n        if missing_context_keys:\n            missing_keys_str = \", \".join([\n                \"\\\"{}\\\"\".format(key) for key in missing_context_keys\n            ])\n            self.log.debug(\"Hook {} skipped. Missing data keys: {}\".format(\n                self.__class__.__name__, missing_keys_str\n            ))\n            return\n\n        timers_manager = self.addons_manager.get(\"timers_manager\")\n        if not timers_manager or not timers_manager.enabled:\n            self.log.info((\n                \"Skipping starting timer because\"\n                \" TimersManager is not available.\"\n            ))\n            return\n\n        timers_manager.start_timer_with_webserver(\n            project_name, folder_path, task_name, logger=self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_timers_manager/plugins/index.html","title":"plugins","text":""},{"location":"autoapi/client/ayon_timers_manager/plugins/publish/index.html","title":"publish","text":""},{"location":"autoapi/client/ayon_timers_manager/plugins/publish/start_timer.html","title":"start_timer","text":"Requires <p>context -&gt; project_settings context -&gt; ayonAddonsManager</p>"},{"location":"autoapi/client/ayon_timers_manager/plugins/publish/stop_timer.html","title":"stop_timer","text":"Requires <p>context -&gt; project_settings context -&gt; ayonAddonsManager</p>"},{"location":"autoapi/server/index.html","title":"server","text":""},{"location":"autoapi/server/settings.html","title":"settings","text":""}]}